//perform simple "AND" operation and right shift the number

int setBits(int n) {
        // Write Your Code here
        int count = 0;
        while(n){
            if(n&1) count++;
            n>>=1;
        }
        return count;
    }
